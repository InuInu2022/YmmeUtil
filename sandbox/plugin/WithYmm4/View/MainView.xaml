<UserControl x:Class="YmmeUtil.Sandbox.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:epoxy="https://github.com/kekyo/Epoxy"
             xmlns:local="clr-namespace:YmmeUtil.Sandbox.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <epoxy:EventBinder.Events>
        <epoxy:Event Command="{Binding Ready}" EventName="Loaded" />
    </epoxy:EventBinder.Events>

    <Grid>
        <StackPanel>
            <Button Content="TaskbarUtil" Command="{Binding TaskbarUtilCommand}" Margin="5,5,5,0" />
            <Button Content="WindowUtil" Command="{Binding WindowUtilCommand}" Margin="5,5,5,0" />
            <Button Content="Button3" Margin="5,5,5,0" />
        </StackPanel>
    </Grid>
</UserControl>
